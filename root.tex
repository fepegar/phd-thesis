% There is a warning about labels that may have changed.
% It seems to be related to the acronym package, but not useful.
% More info at https://tex.stackexchange.com/questions/154594/how-to-diagnose-a-permanent-labels-may-have-changed-warning

\RequirePackage[l2tabu, orthodox]{nag}

% Options need to be in the same line for texcount to work
\documentclass[british,a4paper,11pt,twoside]{StyleThese}
% \documentclass[british,a4paper,11pt,twoside,draft]{StyleThese}

\input{tex/config}
\input{tex/title}

\begin{document}
  \input{tex/frontmatter}
  \input{tex/mainmatter}

  % Need to put this here for texcount's -incbib option to work
  \backmatter
  % Steps to import references:
  % 1. Open Zotero
  % 2. Right-click on "Group Libraries -> phd_fernando -> phd"
  % 3. Select "Export Collection..."
  % 4. Select "BibTeX" as format, all checkboxes unchecked, "Unicode (UTF-8 without BOM)" as encoding
  % 5. Save file as "phd.bib" in the "references" folder. Wait until the library is exported by Zotero
  % 6. Run "python scripts/bold_name_references.py" from the root folder
  \bibliography{references/JNFull, references/phd_bold}
\end{document}

% Trim reminder:
% trim={<left> <lower> <right> <upper>}
